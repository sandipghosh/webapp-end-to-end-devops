name: Deploy Workflow

on:
  workflow_call:

jobs:
  blank-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print hello
        run: echo "Hello from a blank job form deploy workflow!"